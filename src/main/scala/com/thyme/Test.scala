package com.thyme

import com.thyme.extension.ExtensionFunction

object Test {
    def main(args: Array[String]): Unit = {
//        (1 to 114).foreach(x => println(ExtensionFunction.generateTokenNumberWithUUID(x)))
    }
}
